{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build:packages": {
      "outputs": ["dist/**"]
    },
    "lint:packages": {},
    "build:examples": {
      "outputs": [".next/**"]
    },
    "lint:examples": {}
  }
}
